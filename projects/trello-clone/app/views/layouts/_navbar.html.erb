<nav class="navbar navbar-default navbar-inverse" role='navigation'>
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Trello Clone</a>
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#trello-navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="trello-navbar">
      <ul class="nav navbar-nav">
        <li><%= link_to "Home", "/" %></li>
      <% if signed_in? %>
        <li><%= link_to "Sign Out", session_url, :method => :delete %></li>
      <% else %>
        <li class="active"><%= link_to "Sign In", new_session_url %></li>
        <li><%= link_to "Sign up", new_user_url %></li>
      <% end %>
      </ul>
      <% if signed_in? %> -->
        <p class="navbar-text navbar-right pull-right">Signed in as: <%= current_user.email %></p>
      <% end %>
    </div>
  </div>
</nav>